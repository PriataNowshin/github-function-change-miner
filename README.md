# github-function-change-miner
A Python-based data collection pipeline that uses the GitHub REST API to find public Python repositories and extract function-level code changes from commits dated 2023 or earlier, storing repository, file, commit, and full function-version data for research analysis.
